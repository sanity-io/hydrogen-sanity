{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist"]
    },
    "lint": {
      "outputs": [".eslintcache"]
    },
    "typecheck": {
      "outputs": [".tsbuildinfo"]
    }
  }
}
