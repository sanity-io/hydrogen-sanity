export {action, loader} from 'hydrogen-sanity/preview/route';

// If you'd like errors to be handled by the root `ErrorBoundary`,
// provide a default export.
export default function Preview() {
  return null;
}
